const appController = require('./app.controller');
const authController = require('./auth.controller');
const userController = require('./user.controller');

module.exports = { appController, authController,  userController}